services:
  api:
    build: .
    env_file:
      - .env.example
    ports:
      - "8000:8000"
    command: uvicorn smart_grid.api:app --host 0.0.0.0 --port 8000

  project:
    build: .
    env_file:
      - .env.example
    command: python scripts/run_project.py
    profiles:
      - tools
